- name: Find and Upload APK
  run: |
    cd MyApp
    find platforms -name "*.apk" -type f
    mkdir -p apk-output
    find platforms -name "*.apk" -type f -exec cp {} apk-output/ \;

- name: Upload APK
  uses: actions/upload-artifact@v4
  with:
    name: android-apk
    path: MyApp/apk-output
